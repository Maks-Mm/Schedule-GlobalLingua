[
  {
    "id": 1777147666422,
    "title": "ssasasa",
    "teacher": "ssasasa",
    "student": "ssasasa",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "zoom",
    "account": "ssasasa",
    "address": "ssasasa",
    "status": "scheduled",
    "createdAt": "2026-04-25T20:07:46.422Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T20:07:48.126Z"
  },
  {
    "id": 1777151471182,
    "title": "sasa",
    "teacher": "sasa",
    "student": "sasa",
    "datetime": "3333-03-31T01:33:00.000Z",
    "channel": "zoom",
    "account": "sasa",
    "address": "sasa",
    "status": "scheduled",
    "createdAt": "2026-04-25T21:11:11.182Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T21:11:12.641Z"
  },
  {
    "id": 1777153848087,
    "title": "w12121",
    "teacher": "w12121",
    "student": "w12121",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "zoom",
    "account": "w12121",
    "address": "w12121",
    "status": "scheduled",
    "createdAt": "2026-04-25T21:50:48.087Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T21:50:49.619Z"
  },
  {
    "id": 1777156022500,
    "title": "Lesson",
    "teacher": "Maxim",
    "student": "M pryshcheüa",
    "datetime": "2026-12-03T12:25:00.000Z",
    "channel": "whatsapp",
    "account": "Valeria",
    "address": "Berlin",
    "status": "scheduled",
    "createdAt": "2026-04-25T22:27:02.500Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T22:27:03.986Z"
  },
  {
    "id": 1777156640450,
    "title": "22",
    "teacher": "22",
    "student": "22",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "zoom",
    "account": "22",
    "address": "22",
    "status": "scheduled",
    "createdAt": "2026-04-25T22:37:20.450Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T22:37:22.021Z"
  },
  {
    "id": 1777156665100,
    "title": "1212",
    "teacher": "1212",
    "student": "1212",
    "datetime": "3333-03-31T01:33:00.000Z",
    "channel": "zoom",
    "account": "1212",
    "address": "1212",
    "status": "scheduled",
    "createdAt": "2026-04-25T22:37:45.100Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T22:37:46.396Z"
  },
  {
    "id": 1777156666158,
    "title": "1212",
    "teacher": "1212",
    "student": "1212",
    "datetime": "3333-03-31T01:33:00.000Z",
    "channel": "zoom",
    "account": "1212",
    "address": "1212",
    "status": "scheduled",
    "createdAt": "2026-04-25T22:37:46.158Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T22:37:47.493Z"
  },
  {
    "id": 1777156666376,
    "title": "1212",
    "teacher": "1212",
    "student": "1212",
    "datetime": "3333-03-31T01:33:00.000Z",
    "channel": "zoom",
    "account": "1212",
    "address": "1212",
    "status": "scheduled",
    "createdAt": "2026-04-25T22:37:46.376Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T22:37:47.687Z"
  },
  {
    "id": 1777156741979,
    "title": "21212",
    "teacher": "21212",
    "student": "21212",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "meet",
    "account": "21212",
    "address": "21212",
    "status": "scheduled",
    "createdAt": "2026-04-25T22:39:01.979Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T22:39:03.456Z"
  },
  {
    "id": 1777157859831,
    "title": "sasasas",
    "teacher": "sasasas",
    "student": "sasasas",
    "datetime": "4444-04-04T02:44:00.000Z",
    "channel": "zoom",
    "account": "sasasas",
    "address": "sasasas",
    "status": "scheduled",
    "createdAt": "2026-04-25T22:57:39.831Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T22:57:41.366Z"
  },
  {
    "id": 1777159501604,
    "title": "sdsd",
    "teacher": "212121",
    "student": "212121",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "skype",
    "account": "212121",
    "address": "212121",
    "status": "scheduled",
    "createdAt": "2026-04-25T23:25:01.604Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T23:25:03.029Z"
  },
  {
    "id": 1777160710495,
    "title": "asasa",
    "teacher": "asasa",
    "student": "asasa",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "zoom",
    "account": "asasa",
    "address": "asasa",
    "status": "scheduled",
    "createdAt": "2026-04-25T23:45:10.495Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T23:45:11.931Z"
  },
  {
    "id": 1777161543371,
    "title": "12122",
    "teacher": "12122",
    "student": "12122",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "skype",
    "account": "12122",
    "address": "12122",
    "status": "scheduled",
    "createdAt": "2026-04-25T23:59:03.371Z",
    "emailSent": true,
    "emailSentAt": "2026-04-25T23:59:06.271Z"
  },
  {
    "id": 1777162243253,
    "title": "Standort",
    "teacher": "Standort",
    "student": "Standort",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "viber",
    "account": "Standort",
    "address": "Standort",
    "status": "scheduled",
    "createdAt": "2026-04-26T00:10:43.253Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T00:10:44.659Z"
  },
  {
    "id": 1777162499538,
    "title": "/* ===== FIX FOR MOBILE - MAIN AREA VISIBILITY ===== */ @media (max-width: 768px) {   /* Override the problematic main-area constraints */   .main-area {     max-height: none !important;     overflow-y: visible !important;     padding-bottom: 20px !important;   }      /* Ensure event cards are visible */   .event-card-3d {     display: block !important;     visibility: visible !important;   }      /* Remove mask effects that hide content on mobile */   .event-card-3d {     -webkit-mask: none !important;     mask: none !important;   }      /* Fix sidebar on mobile */   .sidebar {     position: static !important;     width: 100% !important;     max-height: none !important;     margin-bottom: 20px;   }      /* Better spacing for mobile */   .event-card-inner {     padding: 16px;   }      .event-title {     font-size: 1rem;   }      .event-actions {     flex-direction: column;     gap: 8px;   }      .event-actions button {     width: 100%;   }      /* Toast positioning fix for mobile */   .toast-modern {     right: 16px;     left: 16px;     bottom: 20px;     text-align: center;   } }",
    "teacher": "/* ===== FIX FOR MOBILE - MAIN AREA VISIBILITY ===== */ @media (max-width: 768px) {   /* Override the problematic main-area constraints */   .main-area {     max-height: none !important;     overflow-y: visible !important;     padding-bottom: 20px !important;   }      /* Ensure event cards are visible */   .event-card-3d {     display: block !important;     visibility: visible !important;   }      /* Remove mask effects that hide content on mobile */   .event-card-3d {     -webkit-mask: none !important;     mask: none !important;   }      /* Fix sidebar on mobile */   .sidebar {     position: static !important;     width: 100% !important;     max-height: none !important;     margin-bottom: 20px;   }      /* Better spacing for mobile */   .event-card-inner {     padding: 16px;   }      .event-title {     font-size: 1rem;   }      .event-actions {     flex-direction: column;     gap: 8px;   }      .event-actions button {     width: 100%;   }      /* Toast positioning fix for mobile */   .toast-modern {     right: 16px;     left: 16px;     bottom: 20px;     text-align: center;   } }",
    "student": "/* ===== FIX FOR MOBILE - MAIN AREA VISIBILITY ===== */ @media (max-width: 768px) {   /* Override the problematic main-area constraints */   .main-area {     max-height: none !important;     overflow-y: visible !important;     padding-bottom: 20px !important;   }      /* Ensure event cards are visible */   .event-card-3d {     display: block !important;     visibility: visible !important;   }      /* Remove mask effects that hide content on mobile */   .event-card-3d {     -webkit-mask: none !important;     mask: none !important;   }      /* Fix sidebar on mobile */   .sidebar {     position: static !important;     width: 100% !important;     max-height: none !important;     margin-bottom: 20px;   }      /* Better spacing for mobile */   .event-card-inner {     padding: 16px;   }      .event-title {     font-size: 1rem;   }      .event-actions {     flex-direction: column;     gap: 8px;   }      .event-actions button {     width: 100%;   }      /* Toast positioning fix for mobile */   .toast-modern {     right: 16px;     left: 16px;     bottom: 20px;     text-align: center;   } }",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "zoom",
    "account": "/* ===== FIX FOR MOBILE - MAIN AREA VISIBILITY ===== */ @media (max-width: 768px) {   /* Override the problematic main-area constraints */   .main-area {     max-height: none !important;     overflow-y: visible !important;     padding-bottom: 20px !important;   }      /* Ensure event cards are visible */   .event-card-3d {     display: block !important;     visibility: visible !important;   }      /* Remove mask effects that hide content on mobile */   .event-card-3d {     -webkit-mask: none !important;     mask: none !important;   }      /* Fix sidebar on mobile */   .sidebar {     position: static !important;     width: 100% !important;     max-height: none !important;     margin-bottom: 20px;   }      /* Better spacing for mobile */   .event-card-inner {     padding: 16px;   }      .event-title {     font-size: 1rem;   }      .event-actions {     flex-direction: column;     gap: 8px;   }      .event-actions button {     width: 100%;   }      /* Toast positioning fix for mobile */   .toast-modern {     right: 16px;     left: 16px;     bottom: 20px;     text-align: center;   } }",
    "address": "/* ===== FIX FOR MOBILE - MAIN AREA VISIBILITY ===== */ @media (max-width: 768px) {   /* Override the problematic main-area constraints */   .main-area {     max-height: none !important;     overflow-y: visible !important;     padding-bottom: 20px !important;   }      /* Ensure event cards are visible */   .event-card-3d {     display: block !important;     visibility: visible !important;   }      /* Remove mask effects that hide content on mobile */   .event-card-3d {     -webkit-mask: none !important;     mask: none !important;   }      /* Fix sidebar on mobile */   .sidebar {     position: static !important;     width: 100% !important;     max-height: none !important;     margin-bottom: 20px;   }      /* Better spacing for mobile */   .event-card-inner {     padding: 16px;   }      .event-title {     font-size: 1rem;   }      .event-actions {     flex-direction: column;     gap: 8px;   }      .event-actions button {     width: 100%;   }      /* Toast positioning fix for mobile */   .toast-modern {     right: 16px;     left: 16px;     bottom: 20px;     text-align: center;   } }",
    "status": "scheduled",
    "createdAt": "2026-04-26T00:14:59.538Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T00:15:01.625Z"
  },
  {
    "id": 1777162930198,
    "title": "sasasasas",
    "teacher": "sasasasas",
    "student": "sasasasas",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "zoom",
    "account": "sasasasas",
    "address": "sasasasas",
    "status": "scheduled",
    "createdAt": "2026-04-26T00:22:10.198Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T00:22:12.508Z"
  },
  {
    "id": 1777163122383,
    "title": "sasasas",
    "teacher": "sasasas",
    "student": "sasasas",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "zoom",
    "account": "sasasas",
    "address": "sasasas",
    "status": "scheduled",
    "createdAt": "2026-04-26T00:25:22.383Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T00:25:24.452Z"
  },
  {
    "id": 1777164912086,
    "title": "122121",
    "teacher": "122121",
    "student": "122121",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "zoom",
    "account": "122121",
    "address": "122121",
    "status": "scheduled",
    "createdAt": "2026-04-26T00:55:12.086Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T00:55:14.134Z"
  },
  {
    "id": 1777230282853,
    "title": "Stundenplan",
    "teacher": "Stundenplan Stundenplan",
    "student": "Stundenplan",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "zoom",
    "account": "Stundenplan",
    "address": "Stundenplan",
    "status": "scheduled",
    "createdAt": "2026-04-26T19:04:42.853Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T19:04:44.806Z"
  },
  {
    "id": 1777230323721,
    "title": "Stundenplan",
    "teacher": "Stundenplan",
    "student": "Stundenplan",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "skype",
    "account": "Stundenplan",
    "address": "Stundenplan",
    "status": "scheduled",
    "createdAt": "2026-04-26T19:05:23.721Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T19:05:25.246Z"
  },
  {
    "id": 1777235880733,
    "title": "Stundenplan",
    "teacher": "Stundenplan",
    "student": "Stundenplan",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "viber",
    "account": "Stundenplan",
    "address": "Stundenplan",
    "status": "scheduled",
    "createdAt": "2026-04-26T20:38:00.733Z",
    "emailSent": true,
    "emailSentAt": "2026-04-26T20:38:03.216Z"
  },
  {
    "id": 1777425806007,
    "title": "Location",
    "teacher": "Location",
    "student": "Location",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "zoom",
    "account": "Location",
    "address": "Location",
    "status": "scheduled",
    "createdAt": "2026-04-29T01:23:26.007Z",
    "emailSent": true,
    "emailSentAt": "2026-04-29T01:23:28.880Z"
  },
  {
    "id": 1777464885221,
    "title": "Schedule",
    "teacher": "Schedule",
    "student": "Schedule",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "viber",
    "account": "Schedule",
    "address": "Schedule",
    "status": "scheduled",
    "createdAt": "2026-04-29T12:14:45.222Z",
    "emailSent": true,
    "emailSentAt": "2026-04-29T12:14:47.337Z"
  },
  {
    "id": 1777470310361,
    "title": "import { useState, useEffect, useRef } from 'react'; import Header from './Header'; import EventCard from './EventCard'; import Sidebar from './Sidebar'; import EventList from './EventList'; import Toast from './Toast'; import ConfirmDialog from './ConfirmDialog'; import { useEvents } from '../hooks/useEvents'; import type { EventFormData } from '../types/LinguaEvent'; import { useLanguage } from '../contexts/LanguageContext';  export default function AppLayout() {   const { t } = useLanguage();    const {     events,     addEvent,     updateEvent,     deleteEvent,     moveEventUp,     moveEventDown   } = useEvents();    const [isMobile, setIsMobile] = useState(() => window.innerWidth <= 768);    useEffect(() => {     const handler = () => setIsMobile(window.innerWidth <= 768);     window.addEventListener('resize', handler);     return () => window.removeEventListener('resize', handler);   }, []);    const [editId, setEditId] = useState<number | null>(null);   const [searchTerm, setSearchTerm] = useState('');   const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null);    const mainAreaRef = useRef<HTMLDivElement>(null);   const sidebarRef = useRef<HTMLDivElement>(null);    const [confirmOpen, setConfirmOpen] = useState(false);   const [onConfirmAction, setOnConfirmAction] = useState<() => void>(() => () => { });    const [successToast, setSuccessToast] = useState(false);   const [lastAddedEventId, setLastAddedEventId] = useState<number | null>(null);    const [mobileView, setMobileView] = useState<'list' | 'form' | 'detail'>('form');   const [selectedEventId, setSelectedEventId] = useState<number | null>(null);    useEffect(() => {     const handleScroll = () => {       const cards = document.querySelectorAll('.event-card-3d');       cards.forEach((card) => {         const rect = card.getBoundingClientRect();         const offset = (rect.top - window.scrollY) * 0.05;         (card as HTMLElement).style.transform = `translateY(${offset}px)`;       });     };      window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    useEffect(() => {     if (lastAddedEventId !== null) {       setTimeout(() => {         const el = document.querySelector(`[data-event-id=\"${lastAddedEventId}\"]`);         if (el) {           el.scrollIntoView({ behavior: 'smooth', block: 'center' });           el.classList.add('highlight-new');           setTimeout(() => el.classList.remove('highlight-new'), 2000);         }         setLastAddedEventId(null);       }, 100);     }   }, [lastAddedEventId, events.length]);    const showToast = (message: string, type: 'success' | 'error' | 'info' = 'success') => {     setToast({ message, type });     setTimeout(() => setToast(null), 3000);   };    const filteredEvents = events.filter(ev =>     searchTerm === '' ||     ev.title.toLowerCase().includes(searchTerm.toLowerCase()) ||     (ev.teacher && ev.teacher.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.student && ev.student.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.account && ev.account.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.address && ev.address.toLowerCase().includes(searchTerm.toLowerCase()))   );    const handleSave = (formData: EventFormData) => {     if (editId !== null) {       updateEvent(editId, formData);       showToast(t.eventUpdated, 'success');       setEditId(null);       if (isMobile) setMobileView('list');     } else {       const newEvent = addEvent(formData);       showToast(t.eventSaved, 'success');       if (newEvent?.id) setLastAddedEventId(newEvent.id);       if (isMobile) setMobileView('list');     }   };    const handleReset = () => {     setEditId(null);     showToast(t.formReset, 'info');   };    const handleEdit = (id: number) => {     setEditId(id);     if (isMobile) setMobileView('form');   };    const handleOpenDetail = (id: number) => {     setSelectedEventId(id);     if (isMobile) setMobileView('detail');   };    const handleMobileBack = () => {     if (mobileView === 'detail') setMobileView('list');     else if (mobileView === 'form') setMobileView('list');     setEditId(null);   };    const handleDelete = (id: number) => {     setOnConfirmAction(() => () => {       deleteEvent(id);       showToast(t.eventDeleted, 'error');       if (editId === id) setEditId(null);     });     setConfirmOpen(true);   };    const handleMoveUp = (index: number) => {     moveEventUp(index);     showToast(t.orderChanged, 'info');   };    const handleMoveDown = (index: number) => {     moveEventDown(index);     showToast(t.orderChanged, 'info');   };    if (isMobile) {     return (       <>         <div className=\"bg-animate\" />         <Header />          <div className=\"mobile-nav\">           {mobileView !== 'list' && (             <button onClick={handleMobileBack} className=\"back-btn\">← Back</button>           )}           {mobileView === 'list' && (             <button onClick={() => { setEditId(null); setMobileView('form'); }} className=\"add-btn\">+ New</button>           )}         </div>          {mobileView === 'form' && (           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         )}          {mobileView === 'list' && (           <div className=\"mobile-list-screen\">             <div className=\"events-header\">               <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>               <input                 type=\"text\"                 placeholder={t.searchPlaceholder}                 value={searchTerm}                 onChange={e => setSearchTerm(e.target.value)}               />             </div>              <EventList               events={filteredEvents}               onEdit={handleEdit}               onDelete={handleDelete}               onMoveUp={handleMoveUp}               onMoveDown={handleMoveDown}               onOpenDetail={handleOpenDetail}             />           </div>         )}          {mobileView === 'detail' && selectedEventId && (           <EventCard             event={events.find(e => e.id === selectedEventId)!}             index={0}             totalEvents={1}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={() => {}}             onMoveDown={() => {}}             onOpenDetail={handleOpenDetail}           />         )}          <ConfirmDialog           isOpen={confirmOpen}           title={t.confirmDeleteTitle}           message={t.confirmDeleteMessage}           onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}           onCancel={() => setConfirmOpen(false)}         />          {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}       </>     );   }    return (     <>       <div className=\"bg-animate\" />       <Header />        <div className=\"app-layout\">         <div className=\"sidebar-container\" ref={sidebarRef}>           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         </div>          <div className=\"main-area\" ref={mainAreaRef}>           <div className=\"events-header\">             <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>             <input               type=\"text\"               placeholder={t.searchPlaceholder}               value={searchTerm}               onChange={e => setSearchTerm(e.target.value)}             />           </div>            <ConfirmDialog             isOpen={confirmOpen}             title={t.confirmDeleteTitle}             message={t.confirmDeleteMessage}             onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}             onCancel={() => setConfirmOpen(false)}           />            <EventList             events={filteredEvents}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={handleMoveUp}             onMoveDown={handleMoveDown}             onOpenDetail={handleOpenDetail}           />         </div>       </div>        {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}     </>   ); }",
    "teacher": "import { useState, useEffect, useRef } from 'react'; import Header from './Header'; import EventCard from './EventCard'; import Sidebar from './Sidebar'; import EventList from './EventList'; import Toast from './Toast'; import ConfirmDialog from './ConfirmDialog'; import { useEvents } from '../hooks/useEvents'; import type { EventFormData } from '../types/LinguaEvent'; import { useLanguage } from '../contexts/LanguageContext';  export default function AppLayout() {   const { t } = useLanguage();    const {     events,     addEvent,     updateEvent,     deleteEvent,     moveEventUp,     moveEventDown   } = useEvents();    const [isMobile, setIsMobile] = useState(() => window.innerWidth <= 768);    useEffect(() => {     const handler = () => setIsMobile(window.innerWidth <= 768);     window.addEventListener('resize', handler);     return () => window.removeEventListener('resize', handler);   }, []);    const [editId, setEditId] = useState<number | null>(null);   const [searchTerm, setSearchTerm] = useState('');   const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null);    const mainAreaRef = useRef<HTMLDivElement>(null);   const sidebarRef = useRef<HTMLDivElement>(null);    const [confirmOpen, setConfirmOpen] = useState(false);   const [onConfirmAction, setOnConfirmAction] = useState<() => void>(() => () => { });    const [successToast, setSuccessToast] = useState(false);   const [lastAddedEventId, setLastAddedEventId] = useState<number | null>(null);    const [mobileView, setMobileView] = useState<'list' | 'form' | 'detail'>('form');   const [selectedEventId, setSelectedEventId] = useState<number | null>(null);    useEffect(() => {     const handleScroll = () => {       const cards = document.querySelectorAll('.event-card-3d');       cards.forEach((card) => {         const rect = card.getBoundingClientRect();         const offset = (rect.top - window.scrollY) * 0.05;         (card as HTMLElement).style.transform = `translateY(${offset}px)`;       });     };      window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    useEffect(() => {     if (lastAddedEventId !== null) {       setTimeout(() => {         const el = document.querySelector(`[data-event-id=\"${lastAddedEventId}\"]`);         if (el) {           el.scrollIntoView({ behavior: 'smooth', block: 'center' });           el.classList.add('highlight-new');           setTimeout(() => el.classList.remove('highlight-new'), 2000);         }         setLastAddedEventId(null);       }, 100);     }   }, [lastAddedEventId, events.length]);    const showToast = (message: string, type: 'success' | 'error' | 'info' = 'success') => {     setToast({ message, type });     setTimeout(() => setToast(null), 3000);   };    const filteredEvents = events.filter(ev =>     searchTerm === '' ||     ev.title.toLowerCase().includes(searchTerm.toLowerCase()) ||     (ev.teacher && ev.teacher.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.student && ev.student.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.account && ev.account.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.address && ev.address.toLowerCase().includes(searchTerm.toLowerCase()))   );    const handleSave = (formData: EventFormData) => {     if (editId !== null) {       updateEvent(editId, formData);       showToast(t.eventUpdated, 'success');       setEditId(null);       if (isMobile) setMobileView('list');     } else {       const newEvent = addEvent(formData);       showToast(t.eventSaved, 'success');       if (newEvent?.id) setLastAddedEventId(newEvent.id);       if (isMobile) setMobileView('list');     }   };    const handleReset = () => {     setEditId(null);     showToast(t.formReset, 'info');   };    const handleEdit = (id: number) => {     setEditId(id);     if (isMobile) setMobileView('form');   };    const handleOpenDetail = (id: number) => {     setSelectedEventId(id);     if (isMobile) setMobileView('detail');   };    const handleMobileBack = () => {     if (mobileView === 'detail') setMobileView('list');     else if (mobileView === 'form') setMobileView('list');     setEditId(null);   };    const handleDelete = (id: number) => {     setOnConfirmAction(() => () => {       deleteEvent(id);       showToast(t.eventDeleted, 'error');       if (editId === id) setEditId(null);     });     setConfirmOpen(true);   };    const handleMoveUp = (index: number) => {     moveEventUp(index);     showToast(t.orderChanged, 'info');   };    const handleMoveDown = (index: number) => {     moveEventDown(index);     showToast(t.orderChanged, 'info');   };    if (isMobile) {     return (       <>         <div className=\"bg-animate\" />         <Header />          <div className=\"mobile-nav\">           {mobileView !== 'list' && (             <button onClick={handleMobileBack} className=\"back-btn\">← Back</button>           )}           {mobileView === 'list' && (             <button onClick={() => { setEditId(null); setMobileView('form'); }} className=\"add-btn\">+ New</button>           )}         </div>          {mobileView === 'form' && (           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         )}          {mobileView === 'list' && (           <div className=\"mobile-list-screen\">             <div className=\"events-header\">               <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>               <input                 type=\"text\"                 placeholder={t.searchPlaceholder}                 value={searchTerm}                 onChange={e => setSearchTerm(e.target.value)}               />             </div>              <EventList               events={filteredEvents}               onEdit={handleEdit}               onDelete={handleDelete}               onMoveUp={handleMoveUp}               onMoveDown={handleMoveDown}               onOpenDetail={handleOpenDetail}             />           </div>         )}          {mobileView === 'detail' && selectedEventId && (           <EventCard             event={events.find(e => e.id === selectedEventId)!}             index={0}             totalEvents={1}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={() => {}}             onMoveDown={() => {}}             onOpenDetail={handleOpenDetail}           />         )}          <ConfirmDialog           isOpen={confirmOpen}           title={t.confirmDeleteTitle}           message={t.confirmDeleteMessage}           onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}           onCancel={() => setConfirmOpen(false)}         />          {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}       </>     );   }    return (     <>       <div className=\"bg-animate\" />       <Header />        <div className=\"app-layout\">         <div className=\"sidebar-container\" ref={sidebarRef}>           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         </div>          <div className=\"main-area\" ref={mainAreaRef}>           <div className=\"events-header\">             <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>             <input               type=\"text\"               placeholder={t.searchPlaceholder}               value={searchTerm}               onChange={e => setSearchTerm(e.target.value)}             />           </div>            <ConfirmDialog             isOpen={confirmOpen}             title={t.confirmDeleteTitle}             message={t.confirmDeleteMessage}             onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}             onCancel={() => setConfirmOpen(false)}           />            <EventList             events={filteredEvents}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={handleMoveUp}             onMoveDown={handleMoveDown}             onOpenDetail={handleOpenDetail}           />         </div>       </div>        {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}     </>   ); }",
    "student": "import { useState, useEffect, useRef } from 'react'; import Header from './Header'; import EventCard from './EventCard'; import Sidebar from './Sidebar'; import EventList from './EventList'; import Toast from './Toast'; import ConfirmDialog from './ConfirmDialog'; import { useEvents } from '../hooks/useEvents'; import type { EventFormData } from '../types/LinguaEvent'; import { useLanguage } from '../contexts/LanguageContext';  export default function AppLayout() {   const { t } = useLanguage();    const {     events,     addEvent,     updateEvent,     deleteEvent,     moveEventUp,     moveEventDown   } = useEvents();    const [isMobile, setIsMobile] = useState(() => window.innerWidth <= 768);    useEffect(() => {     const handler = () => setIsMobile(window.innerWidth <= 768);     window.addEventListener('resize', handler);     return () => window.removeEventListener('resize', handler);   }, []);    const [editId, setEditId] = useState<number | null>(null);   const [searchTerm, setSearchTerm] = useState('');   const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null);    const mainAreaRef = useRef<HTMLDivElement>(null);   const sidebarRef = useRef<HTMLDivElement>(null);    const [confirmOpen, setConfirmOpen] = useState(false);   const [onConfirmAction, setOnConfirmAction] = useState<() => void>(() => () => { });    const [successToast, setSuccessToast] = useState(false);   const [lastAddedEventId, setLastAddedEventId] = useState<number | null>(null);    const [mobileView, setMobileView] = useState<'list' | 'form' | 'detail'>('form');   const [selectedEventId, setSelectedEventId] = useState<number | null>(null);    useEffect(() => {     const handleScroll = () => {       const cards = document.querySelectorAll('.event-card-3d');       cards.forEach((card) => {         const rect = card.getBoundingClientRect();         const offset = (rect.top - window.scrollY) * 0.05;         (card as HTMLElement).style.transform = `translateY(${offset}px)`;       });     };      window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    useEffect(() => {     if (lastAddedEventId !== null) {       setTimeout(() => {         const el = document.querySelector(`[data-event-id=\"${lastAddedEventId}\"]`);         if (el) {           el.scrollIntoView({ behavior: 'smooth', block: 'center' });           el.classList.add('highlight-new');           setTimeout(() => el.classList.remove('highlight-new'), 2000);         }         setLastAddedEventId(null);       }, 100);     }   }, [lastAddedEventId, events.length]);    const showToast = (message: string, type: 'success' | 'error' | 'info' = 'success') => {     setToast({ message, type });     setTimeout(() => setToast(null), 3000);   };    const filteredEvents = events.filter(ev =>     searchTerm === '' ||     ev.title.toLowerCase().includes(searchTerm.toLowerCase()) ||     (ev.teacher && ev.teacher.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.student && ev.student.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.account && ev.account.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.address && ev.address.toLowerCase().includes(searchTerm.toLowerCase()))   );    const handleSave = (formData: EventFormData) => {     if (editId !== null) {       updateEvent(editId, formData);       showToast(t.eventUpdated, 'success');       setEditId(null);       if (isMobile) setMobileView('list');     } else {       const newEvent = addEvent(formData);       showToast(t.eventSaved, 'success');       if (newEvent?.id) setLastAddedEventId(newEvent.id);       if (isMobile) setMobileView('list');     }   };    const handleReset = () => {     setEditId(null);     showToast(t.formReset, 'info');   };    const handleEdit = (id: number) => {     setEditId(id);     if (isMobile) setMobileView('form');   };    const handleOpenDetail = (id: number) => {     setSelectedEventId(id);     if (isMobile) setMobileView('detail');   };    const handleMobileBack = () => {     if (mobileView === 'detail') setMobileView('list');     else if (mobileView === 'form') setMobileView('list');     setEditId(null);   };    const handleDelete = (id: number) => {     setOnConfirmAction(() => () => {       deleteEvent(id);       showToast(t.eventDeleted, 'error');       if (editId === id) setEditId(null);     });     setConfirmOpen(true);   };    const handleMoveUp = (index: number) => {     moveEventUp(index);     showToast(t.orderChanged, 'info');   };    const handleMoveDown = (index: number) => {     moveEventDown(index);     showToast(t.orderChanged, 'info');   };    if (isMobile) {     return (       <>         <div className=\"bg-animate\" />         <Header />          <div className=\"mobile-nav\">           {mobileView !== 'list' && (             <button onClick={handleMobileBack} className=\"back-btn\">← Back</button>           )}           {mobileView === 'list' && (             <button onClick={() => { setEditId(null); setMobileView('form'); }} className=\"add-btn\">+ New</button>           )}         </div>          {mobileView === 'form' && (           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         )}          {mobileView === 'list' && (           <div className=\"mobile-list-screen\">             <div className=\"events-header\">               <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>               <input                 type=\"text\"                 placeholder={t.searchPlaceholder}                 value={searchTerm}                 onChange={e => setSearchTerm(e.target.value)}               />             </div>              <EventList               events={filteredEvents}               onEdit={handleEdit}               onDelete={handleDelete}               onMoveUp={handleMoveUp}               onMoveDown={handleMoveDown}               onOpenDetail={handleOpenDetail}             />           </div>         )}          {mobileView === 'detail' && selectedEventId && (           <EventCard             event={events.find(e => e.id === selectedEventId)!}             index={0}             totalEvents={1}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={() => {}}             onMoveDown={() => {}}             onOpenDetail={handleOpenDetail}           />         )}          <ConfirmDialog           isOpen={confirmOpen}           title={t.confirmDeleteTitle}           message={t.confirmDeleteMessage}           onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}           onCancel={() => setConfirmOpen(false)}         />          {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}       </>     );   }    return (     <>       <div className=\"bg-animate\" />       <Header />        <div className=\"app-layout\">         <div className=\"sidebar-container\" ref={sidebarRef}>           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         </div>          <div className=\"main-area\" ref={mainAreaRef}>           <div className=\"events-header\">             <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>             <input               type=\"text\"               placeholder={t.searchPlaceholder}               value={searchTerm}               onChange={e => setSearchTerm(e.target.value)}             />           </div>            <ConfirmDialog             isOpen={confirmOpen}             title={t.confirmDeleteTitle}             message={t.confirmDeleteMessage}             onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}             onCancel={() => setConfirmOpen(false)}           />            <EventList             events={filteredEvents}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={handleMoveUp}             onMoveDown={handleMoveDown}             onOpenDetail={handleOpenDetail}           />         </div>       </div>        {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}     </>   ); }",
    "datetime": "3333-03-31T01:33:00.000Z",
    "channel": "in-person",
    "account": "import { useState, useEffect, useRef } from 'react'; import Header from './Header'; import EventCard from './EventCard'; import Sidebar from './Sidebar'; import EventList from './EventList'; import Toast from './Toast'; import ConfirmDialog from './ConfirmDialog'; import { useEvents } from '../hooks/useEvents'; import type { EventFormData } from '../types/LinguaEvent'; import { useLanguage } from '../contexts/LanguageContext';  export default function AppLayout() {   const { t } = useLanguage();    const {     events,     addEvent,     updateEvent,     deleteEvent,     moveEventUp,     moveEventDown   } = useEvents();    const [isMobile, setIsMobile] = useState(() => window.innerWidth <= 768);    useEffect(() => {     const handler = () => setIsMobile(window.innerWidth <= 768);     window.addEventListener('resize', handler);     return () => window.removeEventListener('resize', handler);   }, []);    const [editId, setEditId] = useState<number | null>(null);   const [searchTerm, setSearchTerm] = useState('');   const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null);    const mainAreaRef = useRef<HTMLDivElement>(null);   const sidebarRef = useRef<HTMLDivElement>(null);    const [confirmOpen, setConfirmOpen] = useState(false);   const [onConfirmAction, setOnConfirmAction] = useState<() => void>(() => () => { });    const [successToast, setSuccessToast] = useState(false);   const [lastAddedEventId, setLastAddedEventId] = useState<number | null>(null);    const [mobileView, setMobileView] = useState<'list' | 'form' | 'detail'>('form');   const [selectedEventId, setSelectedEventId] = useState<number | null>(null);    useEffect(() => {     const handleScroll = () => {       const cards = document.querySelectorAll('.event-card-3d');       cards.forEach((card) => {         const rect = card.getBoundingClientRect();         const offset = (rect.top - window.scrollY) * 0.05;         (card as HTMLElement).style.transform = `translateY(${offset}px)`;       });     };      window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    useEffect(() => {     if (lastAddedEventId !== null) {       setTimeout(() => {         const el = document.querySelector(`[data-event-id=\"${lastAddedEventId}\"]`);         if (el) {           el.scrollIntoView({ behavior: 'smooth', block: 'center' });           el.classList.add('highlight-new');           setTimeout(() => el.classList.remove('highlight-new'), 2000);         }         setLastAddedEventId(null);       }, 100);     }   }, [lastAddedEventId, events.length]);    const showToast = (message: string, type: 'success' | 'error' | 'info' = 'success') => {     setToast({ message, type });     setTimeout(() => setToast(null), 3000);   };    const filteredEvents = events.filter(ev =>     searchTerm === '' ||     ev.title.toLowerCase().includes(searchTerm.toLowerCase()) ||     (ev.teacher && ev.teacher.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.student && ev.student.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.account && ev.account.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.address && ev.address.toLowerCase().includes(searchTerm.toLowerCase()))   );    const handleSave = (formData: EventFormData) => {     if (editId !== null) {       updateEvent(editId, formData);       showToast(t.eventUpdated, 'success');       setEditId(null);       if (isMobile) setMobileView('list');     } else {       const newEvent = addEvent(formData);       showToast(t.eventSaved, 'success');       if (newEvent?.id) setLastAddedEventId(newEvent.id);       if (isMobile) setMobileView('list');     }   };    const handleReset = () => {     setEditId(null);     showToast(t.formReset, 'info');   };    const handleEdit = (id: number) => {     setEditId(id);     if (isMobile) setMobileView('form');   };    const handleOpenDetail = (id: number) => {     setSelectedEventId(id);     if (isMobile) setMobileView('detail');   };    const handleMobileBack = () => {     if (mobileView === 'detail') setMobileView('list');     else if (mobileView === 'form') setMobileView('list');     setEditId(null);   };    const handleDelete = (id: number) => {     setOnConfirmAction(() => () => {       deleteEvent(id);       showToast(t.eventDeleted, 'error');       if (editId === id) setEditId(null);     });     setConfirmOpen(true);   };    const handleMoveUp = (index: number) => {     moveEventUp(index);     showToast(t.orderChanged, 'info');   };    const handleMoveDown = (index: number) => {     moveEventDown(index);     showToast(t.orderChanged, 'info');   };    if (isMobile) {     return (       <>         <div className=\"bg-animate\" />         <Header />          <div className=\"mobile-nav\">           {mobileView !== 'list' && (             <button onClick={handleMobileBack} className=\"back-btn\">← Back</button>           )}           {mobileView === 'list' && (             <button onClick={() => { setEditId(null); setMobileView('form'); }} className=\"add-btn\">+ New</button>           )}         </div>          {mobileView === 'form' && (           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         )}          {mobileView === 'list' && (           <div className=\"mobile-list-screen\">             <div className=\"events-header\">               <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>               <input                 type=\"text\"                 placeholder={t.searchPlaceholder}                 value={searchTerm}                 onChange={e => setSearchTerm(e.target.value)}               />             </div>              <EventList               events={filteredEvents}               onEdit={handleEdit}               onDelete={handleDelete}               onMoveUp={handleMoveUp}               onMoveDown={handleMoveDown}               onOpenDetail={handleOpenDetail}             />           </div>         )}          {mobileView === 'detail' && selectedEventId && (           <EventCard             event={events.find(e => e.id === selectedEventId)!}             index={0}             totalEvents={1}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={() => {}}             onMoveDown={() => {}}             onOpenDetail={handleOpenDetail}           />         )}          <ConfirmDialog           isOpen={confirmOpen}           title={t.confirmDeleteTitle}           message={t.confirmDeleteMessage}           onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}           onCancel={() => setConfirmOpen(false)}         />          {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}       </>     );   }    return (     <>       <div className=\"bg-animate\" />       <Header />        <div className=\"app-layout\">         <div className=\"sidebar-container\" ref={sidebarRef}>           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         </div>          <div className=\"main-area\" ref={mainAreaRef}>           <div className=\"events-header\">             <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>             <input               type=\"text\"               placeholder={t.searchPlaceholder}               value={searchTerm}               onChange={e => setSearchTerm(e.target.value)}             />           </div>            <ConfirmDialog             isOpen={confirmOpen}             title={t.confirmDeleteTitle}             message={t.confirmDeleteMessage}             onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}             onCancel={() => setConfirmOpen(false)}           />            <EventList             events={filteredEvents}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={handleMoveUp}             onMoveDown={handleMoveDown}             onOpenDetail={handleOpenDetail}           />         </div>       </div>        {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}     </>   ); }import { useState, useEffect, useRef } from 'react'; import Header from './Header'; import EventCard from './EventCard'; import Sidebar from './Sidebar'; import EventList from './EventList'; import Toast from './Toast'; import ConfirmDialog from './ConfirmDialog'; import { useEvents } from '../hooks/useEvents'; import type { EventFormData } from '../types/LinguaEvent'; import { useLanguage } from '../contexts/LanguageContext';  export default function AppLayout() {   const { t } = useLanguage();    const {     events,     addEvent,     updateEvent,     deleteEvent,     moveEventUp,     moveEventDown   } = useEvents();    const [isMobile, setIsMobile] = useState(() => window.innerWidth <= 768);    useEffect(() => {     const handler = () => setIsMobile(window.innerWidth <= 768);     window.addEventListener('resize', handler);     return () => window.removeEventListener('resize', handler);   }, []);    const [editId, setEditId] = useState<number | null>(null);   const [searchTerm, setSearchTerm] = useState('');   const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null);    const mainAreaRef = useRef<HTMLDivElement>(null);   const sidebarRef = useRef<HTMLDivElement>(null);    const [confirmOpen, setConfirmOpen] = useState(false);   const [onConfirmAction, setOnConfirmAction] = useState<() => void>(() => () => { });    const [successToast, setSuccessToast] = useState(false);   const [lastAddedEventId, setLastAddedEventId] = useState<number | null>(null);    const [mobileView, setMobileView] = useState<'list' | 'form' | 'detail'>('form');   const [selectedEventId, setSelectedEventId] = useState<number | null>(null);    useEffect(() => {     const handleScroll = () => {       const cards = document.querySelectorAll('.event-card-3d');       cards.forEach((card) => {         const rect = card.getBoundingClientRect();         const offset = (rect.top - window.scrollY) * 0.05;         (card as HTMLElement).style.transform = `translateY(${offset}px)`;       });     };      window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    useEffect(() => {     if (lastAddedEventId !== null) {       setTimeout(() => {         const el = document.querySelector(`[data-event-id=\"${lastAddedEventId}\"]`);         if (el) {           el.scrollIntoView({ behavior: 'smooth', block: 'center' });           el.classList.add('highlight-new');           setTimeout(() => el.classList.remove('highlight-new'), 2000);         }         setLastAddedEventId(null);       }, 100);     }   }, [lastAddedEventId, events.length]);    const showToast = (message: string, type: 'success' | 'error' | 'info' = 'success') => {     setToast({ message, type });     setTimeout(() => setToast(null), 3000);   };    const filteredEvents = events.filter(ev =>     searchTerm === '' ||     ev.title.toLowerCase().includes(searchTerm.toLowerCase()) ||     (ev.teacher && ev.teacher.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.student && ev.student.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.account && ev.account.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.address && ev.address.toLowerCase().includes(searchTerm.toLowerCase()))   );    const handleSave = (formData: EventFormData) => {     if (editId !== null) {       updateEvent(editId, formData);       showToast(t.eventUpdated, 'success');       setEditId(null);       if (isMobile) setMobileView('list');     } else {       const newEvent = addEvent(formData);       showToast(t.eventSaved, 'success');       if (newEvent?.id) setLastAddedEventId(newEvent.id);       if (isMobile) setMobileView('list');     }   };    const handleReset = () => {     setEditId(null);     showToast(t.formReset, 'info');   };    const handleEdit = (id: number) => {     setEditId(id);     if (isMobile) setMobileView('form');   };    const handleOpenDetail = (id: number) => {     setSelectedEventId(id);     if (isMobile) setMobileView('detail');   };    const handleMobileBack = () => {     if (mobileView === 'detail') setMobileView('list');     else if (mobileView === 'form') setMobileView('list');     setEditId(null);   };    const handleDelete = (id: number) => {     setOnConfirmAction(() => () => {       deleteEvent(id);       showToast(t.eventDeleted, 'error');       if (editId === id) setEditId(null);     });     setConfirmOpen(true);   };    const handleMoveUp = (index: number) => {     moveEventUp(index);     showToast(t.orderChanged, 'info');   };    const handleMoveDown = (index: number) => {     moveEventDown(index);     showToast(t.orderChanged, 'info');   };    if (isMobile) {     return (       <>         <div className=\"bg-animate\" />         <Header />          <div className=\"mobile-nav\">           {mobileView !== 'list' && (             <button onClick={handleMobileBack} className=\"back-btn\">← Back</button>           )}           {mobileView === 'list' && (             <button onClick={() => { setEditId(null); setMobileView('form'); }} className=\"add-btn\">+ New</button>           )}         </div>          {mobileView === 'form' && (           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         )}          {mobileView === 'list' && (           <div className=\"mobile-list-screen\">             <div className=\"events-header\">               <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>               <input                 type=\"text\"                 placeholder={t.searchPlaceholder}                 value={searchTerm}                 onChange={e => setSearchTerm(e.target.value)}               />             </div>              <EventList               events={filteredEvents}               onEdit={handleEdit}               onDelete={handleDelete}               onMoveUp={handleMoveUp}               onMoveDown={handleMoveDown}               onOpenDetail={handleOpenDetail}             />           </div>         )}          {mobileView === 'detail' && selectedEventId && (           <EventCard             event={events.find(e => e.id === selectedEventId)!}             index={0}             totalEvents={1}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={() => {}}             onMoveDown={() => {}}             onOpenDetail={handleOpenDetail}           />         )}          <ConfirmDialog           isOpen={confirmOpen}           title={t.confirmDeleteTitle}           message={t.confirmDeleteMessage}           onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}           onCancel={() => setConfirmOpen(false)}         />          {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}       </>     );   }    return (     <>       <div className=\"bg-animate\" />       <Header />        <div className=\"app-layout\">         <div className=\"sidebar-container\" ref={sidebarRef}>           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         </div>          <div className=\"main-area\" ref={mainAreaRef}>           <div className=\"events-header\">             <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>             <input               type=\"text\"               placeholder={t.searchPlaceholder}               value={searchTerm}               onChange={e => setSearchTerm(e.target.value)}             />           </div>            <ConfirmDialog             isOpen={confirmOpen}             title={t.confirmDeleteTitle}             message={t.confirmDeleteMessage}             onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}             onCancel={() => setConfirmOpen(false)}           />            <EventList             events={filteredEvents}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={handleMoveUp}             onMoveDown={handleMoveDown}             onOpenDetail={handleOpenDetail}           />         </div>       </div>        {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}     </>   ); }",
    "address": "import { useState, useEffect, useRef } from 'react'; import Header from './Header'; import EventCard from './EventCard'; import Sidebar from './Sidebar'; import EventList from './EventList'; import Toast from './Toast'; import ConfirmDialog from './ConfirmDialog'; import { useEvents } from '../hooks/useEvents'; import type { EventFormData } from '../types/LinguaEvent'; import { useLanguage } from '../contexts/LanguageContext';  export default function AppLayout() {   const { t } = useLanguage();    const {     events,     addEvent,     updateEvent,     deleteEvent,     moveEventUp,     moveEventDown   } = useEvents();    const [isMobile, setIsMobile] = useState(() => window.innerWidth <= 768);    useEffect(() => {     const handler = () => setIsMobile(window.innerWidth <= 768);     window.addEventListener('resize', handler);     return () => window.removeEventListener('resize', handler);   }, []);    const [editId, setEditId] = useState<number | null>(null);   const [searchTerm, setSearchTerm] = useState('');   const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null);    const mainAreaRef = useRef<HTMLDivElement>(null);   const sidebarRef = useRef<HTMLDivElement>(null);    const [confirmOpen, setConfirmOpen] = useState(false);   const [onConfirmAction, setOnConfirmAction] = useState<() => void>(() => () => { });    const [successToast, setSuccessToast] = useState(false);   const [lastAddedEventId, setLastAddedEventId] = useState<number | null>(null);    const [mobileView, setMobileView] = useState<'list' | 'form' | 'detail'>('form');   const [selectedEventId, setSelectedEventId] = useState<number | null>(null);    useEffect(() => {     const handleScroll = () => {       const cards = document.querySelectorAll('.event-card-3d');       cards.forEach((card) => {         const rect = card.getBoundingClientRect();         const offset = (rect.top - window.scrollY) * 0.05;         (card as HTMLElement).style.transform = `translateY(${offset}px)`;       });     };      window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    useEffect(() => {     if (lastAddedEventId !== null) {       setTimeout(() => {         const el = document.querySelector(`[data-event-id=\"${lastAddedEventId}\"]`);         if (el) {           el.scrollIntoView({ behavior: 'smooth', block: 'center' });           el.classList.add('highlight-new');           setTimeout(() => el.classList.remove('highlight-new'), 2000);         }         setLastAddedEventId(null);       }, 100);     }   }, [lastAddedEventId, events.length]);    const showToast = (message: string, type: 'success' | 'error' | 'info' = 'success') => {     setToast({ message, type });     setTimeout(() => setToast(null), 3000);   };    const filteredEvents = events.filter(ev =>     searchTerm === '' ||     ev.title.toLowerCase().includes(searchTerm.toLowerCase()) ||     (ev.teacher && ev.teacher.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.student && ev.student.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.account && ev.account.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.address && ev.address.toLowerCase().includes(searchTerm.toLowerCase()))   );    const handleSave = (formData: EventFormData) => {     if (editId !== null) {       updateEvent(editId, formData);       showToast(t.eventUpdated, 'success');       setEditId(null);       if (isMobile) setMobileView('list');     } else {       const newEvent = addEvent(formData);       showToast(t.eventSaved, 'success');       if (newEvent?.id) setLastAddedEventId(newEvent.id);       if (isMobile) setMobileView('list');     }   };    const handleReset = () => {     setEditId(null);     showToast(t.formReset, 'info');   };    const handleEdit = (id: number) => {     setEditId(id);     if (isMobile) setMobileView('form');   };    const handleOpenDetail = (id: number) => {     setSelectedEventId(id);     if (isMobile) setMobileView('detail');   };    const handleMobileBack = () => {     if (mobileView === 'detail') setMobileView('list');     else if (mobileView === 'form') setMobileView('list');     setEditId(null);   };    const handleDelete = (id: number) => {     setOnConfirmAction(() => () => {       deleteEvent(id);       showToast(t.eventDeleted, 'error');       if (editId === id) setEditId(null);     });     setConfirmOpen(true);   };    const handleMoveUp = (index: number) => {     moveEventUp(index);     showToast(t.orderChanged, 'info');   };    const handleMoveDown = (index: number) => {     moveEventDown(index);     showToast(t.orderChanged, 'info');   };    if (isMobile) {     return (       <>         <div className=\"bg-animate\" />         <Header />          <div className=\"mobile-nav\">           {mobileView !== 'list' && (             <button onClick={handleMobileBack} className=\"back-btn\">← Back</button>           )}           {mobileView === 'list' && (             <button onClick={() => { setEditId(null); setMobileView('form'); }} className=\"add-btn\">+ New</button>           )}         </div>          {mobileView === 'form' && (           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         )}          {mobileView === 'list' && (           <div className=\"mobile-list-screen\">             <div className=\"events-header\">               <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>               <input                 type=\"text\"                 placeholder={t.searchPlaceholder}                 value={searchTerm}                 onChange={e => setSearchTerm(e.target.value)}               />             </div>              <EventList               events={filteredEvents}               onEdit={handleEdit}               onDelete={handleDelete}               onMoveUp={handleMoveUp}               onMoveDown={handleMoveDown}               onOpenDetail={handleOpenDetail}             />           </div>         )}          {mobileView === 'detail' && selectedEventId && (           <EventCard             event={events.find(e => e.id === selectedEventId)!}             index={0}             totalEvents={1}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={() => {}}             onMoveDown={() => {}}             onOpenDetail={handleOpenDetail}           />         )}          <ConfirmDialog           isOpen={confirmOpen}           title={t.confirmDeleteTitle}           message={t.confirmDeleteMessage}           onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}           onCancel={() => setConfirmOpen(false)}         />          {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}       </>     );   }    return (     <>       <div className=\"bg-animate\" />       <Header />        <div className=\"app-layout\">         <div className=\"sidebar-container\" ref={sidebarRef}>           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         </div>          <div className=\"main-area\" ref={mainAreaRef}>           <div className=\"events-header\">             <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>             <input               type=\"text\"               placeholder={t.searchPlaceholder}               value={searchTerm}               onChange={e => setSearchTerm(e.target.value)}             />           </div>            <ConfirmDialog             isOpen={confirmOpen}             title={t.confirmDeleteTitle}             message={t.confirmDeleteMessage}             onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}             onCancel={() => setConfirmOpen(false)}           />            <EventList             events={filteredEvents}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={handleMoveUp}             onMoveDown={handleMoveDown}             onOpenDetail={handleOpenDetail}           />         </div>       </div>        {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}     </>   ); }import { useState, useEffect, useRef } from 'react'; import Header from './Header'; import EventCard from './EventCard'; import Sidebar from './Sidebar'; import EventList from './EventList'; import Toast from './Toast'; import ConfirmDialog from './ConfirmDialog'; import { useEvents } from '../hooks/useEvents'; import type { EventFormData } from '../types/LinguaEvent'; import { useLanguage } from '../contexts/LanguageContext';  export default function AppLayout() {   const { t } = useLanguage();    const {     events,     addEvent,     updateEvent,     deleteEvent,     moveEventUp,     moveEventDown   } = useEvents();    const [isMobile, setIsMobile] = useState(() => window.innerWidth <= 768);    useEffect(() => {     const handler = () => setIsMobile(window.innerWidth <= 768);     window.addEventListener('resize', handler);     return () => window.removeEventListener('resize', handler);   }, []);    const [editId, setEditId] = useState<number | null>(null);   const [searchTerm, setSearchTerm] = useState('');   const [toast, setToast] = useState<{ message: string; type: 'success' | 'error' | 'info' } | null>(null);    const mainAreaRef = useRef<HTMLDivElement>(null);   const sidebarRef = useRef<HTMLDivElement>(null);    const [confirmOpen, setConfirmOpen] = useState(false);   const [onConfirmAction, setOnConfirmAction] = useState<() => void>(() => () => { });    const [successToast, setSuccessToast] = useState(false);   const [lastAddedEventId, setLastAddedEventId] = useState<number | null>(null);    const [mobileView, setMobileView] = useState<'list' | 'form' | 'detail'>('form');   const [selectedEventId, setSelectedEventId] = useState<number | null>(null);    useEffect(() => {     const handleScroll = () => {       const cards = document.querySelectorAll('.event-card-3d');       cards.forEach((card) => {         const rect = card.getBoundingClientRect();         const offset = (rect.top - window.scrollY) * 0.05;         (card as HTMLElement).style.transform = `translateY(${offset}px)`;       });     };      window.addEventListener('scroll', handleScroll);     return () => window.removeEventListener('scroll', handleScroll);   }, []);    useEffect(() => {     if (lastAddedEventId !== null) {       setTimeout(() => {         const el = document.querySelector(`[data-event-id=\"${lastAddedEventId}\"]`);         if (el) {           el.scrollIntoView({ behavior: 'smooth', block: 'center' });           el.classList.add('highlight-new');           setTimeout(() => el.classList.remove('highlight-new'), 2000);         }         setLastAddedEventId(null);       }, 100);     }   }, [lastAddedEventId, events.length]);    const showToast = (message: string, type: 'success' | 'error' | 'info' = 'success') => {     setToast({ message, type });     setTimeout(() => setToast(null), 3000);   };    const filteredEvents = events.filter(ev =>     searchTerm === '' ||     ev.title.toLowerCase().includes(searchTerm.toLowerCase()) ||     (ev.teacher && ev.teacher.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.student && ev.student.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.account && ev.account.toLowerCase().includes(searchTerm.toLowerCase())) ||     (ev.address && ev.address.toLowerCase().includes(searchTerm.toLowerCase()))   );    const handleSave = (formData: EventFormData) => {     if (editId !== null) {       updateEvent(editId, formData);       showToast(t.eventUpdated, 'success');       setEditId(null);       if (isMobile) setMobileView('list');     } else {       const newEvent = addEvent(formData);       showToast(t.eventSaved, 'success');       if (newEvent?.id) setLastAddedEventId(newEvent.id);       if (isMobile) setMobileView('list');     }   };    const handleReset = () => {     setEditId(null);     showToast(t.formReset, 'info');   };    const handleEdit = (id: number) => {     setEditId(id);     if (isMobile) setMobileView('form');   };    const handleOpenDetail = (id: number) => {     setSelectedEventId(id);     if (isMobile) setMobileView('detail');   };    const handleMobileBack = () => {     if (mobileView === 'detail') setMobileView('list');     else if (mobileView === 'form') setMobileView('list');     setEditId(null);   };    const handleDelete = (id: number) => {     setOnConfirmAction(() => () => {       deleteEvent(id);       showToast(t.eventDeleted, 'error');       if (editId === id) setEditId(null);     });     setConfirmOpen(true);   };    const handleMoveUp = (index: number) => {     moveEventUp(index);     showToast(t.orderChanged, 'info');   };    const handleMoveDown = (index: number) => {     moveEventDown(index);     showToast(t.orderChanged, 'info');   };    if (isMobile) {     return (       <>         <div className=\"bg-animate\" />         <Header />          <div className=\"mobile-nav\">           {mobileView !== 'list' && (             <button onClick={handleMobileBack} className=\"back-btn\">← Back</button>           )}           {mobileView === 'list' && (             <button onClick={() => { setEditId(null); setMobileView('form'); }} className=\"add-btn\">+ New</button>           )}         </div>          {mobileView === 'form' && (           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         )}          {mobileView === 'list' && (           <div className=\"mobile-list-screen\">             <div className=\"events-header\">               <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>               <input                 type=\"text\"                 placeholder={t.searchPlaceholder}                 value={searchTerm}                 onChange={e => setSearchTerm(e.target.value)}               />             </div>              <EventList               events={filteredEvents}               onEdit={handleEdit}               onDelete={handleDelete}               onMoveUp={handleMoveUp}               onMoveDown={handleMoveDown}               onOpenDetail={handleOpenDetail}             />           </div>         )}          {mobileView === 'detail' && selectedEventId && (           <EventCard             event={events.find(e => e.id === selectedEventId)!}             index={0}             totalEvents={1}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={() => {}}             onMoveDown={() => {}}             onOpenDetail={handleOpenDetail}           />         )}          <ConfirmDialog           isOpen={confirmOpen}           title={t.confirmDeleteTitle}           message={t.confirmDeleteMessage}           onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}           onCancel={() => setConfirmOpen(false)}         />          {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}       </>     );   }    return (     <>       <div className=\"bg-animate\" />       <Header />        <div className=\"app-layout\">         <div className=\"sidebar-container\" ref={sidebarRef}>           <Sidebar editId={editId} events={events} onSave={handleSave} onReset={handleReset} />         </div>          <div className=\"main-area\" ref={mainAreaRef}>           <div className=\"events-header\">             <h3>{t.scheduleWithCount.replace('{{count}}', String(filteredEvents.length))}</h3>             <input               type=\"text\"               placeholder={t.searchPlaceholder}               value={searchTerm}               onChange={e => setSearchTerm(e.target.value)}             />           </div>            <ConfirmDialog             isOpen={confirmOpen}             title={t.confirmDeleteTitle}             message={t.confirmDeleteMessage}             onConfirm={() => { onConfirmAction(); setConfirmOpen(false); }}             onCancel={() => setConfirmOpen(false)}           />            <EventList             events={filteredEvents}             onEdit={handleEdit}             onDelete={handleDelete}             onMoveUp={handleMoveUp}             onMoveDown={handleMoveDown}             onOpenDetail={handleOpenDetail}           />         </div>       </div>        {toast && <Toast message={toast.message} type={toast.type} onClose={() => setToast(null)} />}     </>   ); }",
    "status": "scheduled",
    "createdAt": "2026-04-29T13:45:10.361Z",
    "emailSent": true,
    "emailSentAt": "2026-04-29T13:45:12.685Z"
  },
  {
    "id": 1777470366609,
    "title": "Location",
    "teacher": "Location",
    "student": "Location",
    "datetime": "1111-11-11T10:17:32.000Z",
    "channel": "in-person",
    "account": "Location",
    "address": "Location",
    "status": "scheduled",
    "createdAt": "2026-04-29T13:46:06.609Z",
    "emailSent": true,
    "emailSentAt": "2026-04-29T13:46:09.660Z"
  },
  {
    "id": 1777478445699,
    "title": "Location",
    "teacher": "Location",
    "student": "Location",
    "datetime": "2222-02-22T21:22:00.000Z",
    "channel": "meet",
    "account": "Location",
    "address": "Location",
    "status": "scheduled",
    "createdAt": "2026-04-29T16:00:45.699Z",
    "emailSent": true,
    "emailSentAt": "2026-04-29T16:00:47.522Z"
  },
  {
    "id": 1777479033351,
    "title": "Account",
    "teacher": "Account",
    "student": "Account",
    "datetime": "1111-02-11T10:17:32.000Z",
    "channel": "meet",
    "account": "Account",
    "address": "Account",
    "status": "scheduled",
    "createdAt": "2026-04-29T16:10:33.351Z",
    "emailSent": true,
    "emailSentAt": "2026-04-29T16:10:34.815Z"
  }
]